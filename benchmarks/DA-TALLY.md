# DA tally — arch-bunny

The Devil's-Advocate gate from `~/github/nix-bunne/benchmarks/METHODOLOGY.md`,
adopted into this repo 2026-08-23 on the author's instruction ("use the
Devil's Advocate methodology outlined in ~/github/nix-bunne for your
testing"). Briefing text: nix-bunne `DA-BRIEFING.md`, used verbatim. Every
conclusion that changes a `CHOICES.md` row or a shipped config clears this
gate first; verbatim transcripts live in `da-logs/`.

| Round | Test | Conclusion | Verdict | Log |
|---|---|---|---|---|
| 1 | 4.1 snapshot cap | "the byte-cap mechanism WORKS" (250 MB canary) | **rejected outright** — floor/space stop ambiguity; production trigger path never observed joining qgroup; n=1 | `da-logs/4.1.snapshot-cap.md` |
| 2 | 4.1 snapshot cap (rerun: 22 GiB @ production limit, floor 0) | scoped: "eventual byte-cap on number-class snapshots, over-deletion error direction" | **survives with caveats** — FREE_LIMIT tested at synthetic threshold only; overshoot causal attribution partially inferred; n=2 runs on 1 install; concurrency/timer-unit/IMPORTANT-class untested | `da-logs/4.1.snapshot-cap.md` |
| 3 | 4.2 Arch vs NixOS (rev 1) | same-instrument cross-OS bake-off | **rejected outright** — false N label, missing dispersion, undelivered niri.service metric, unproven instrument parity, no pre-registered thresholds | `da-logs/4.2.arch-vs-nixos.md` |
| 4 | 4.2 (rev 2) | characterization only; explicitly does not close `os-base` | **rejected outright** — forensic cross-check: unbacked niri.service/kernel/loader numbers, sample-1 cell under sample-3 header (real niri gap ~13%), false load envelope, transcribed firmware range | `da-logs/4.2.arch-vs-nixos.md` |
| 5 | 4.2 (rev 3) | same scoped claim; every number raw-backed after evidence-capture round trip | **survives with caveats** — all r2 fixes verified against raw; 3 residual supporting-material defects, fixed in place same night | `da-logs/4.2.arch-vs-nixos.md` |
| 6 | 4.5 docker quota (rev 1) | "a write was actually refused" (rows' own criterion) | **rejected outright** — load-bearing claims unlogged (n=1), migration path tested instead of the install recipe, snapper-vs-consistency interaction unchecked | `da-logs/4.5.docker-quota.md` |
| 7 | 4.5 (rev 2, after 4.5b rerun) | scoped: cap enforces post-rescan (1 tee'd docker-path refusal + n=2 fresh-subvol recipe); survives a snapper snapshot (n=1); wedge + recovery tee'd; GC residue grows per delete-cycle | **survives with caveats** — text-honesty fixes applied in place; no further rerun demanded | `da-logs/4.5.docker-quota.md` |

| 8 | 4.8 kitty post-nvidia (rev 1) | "author's cold-launch hypothesis refuted, sign reversed; ~11 ms glvnd tax; 4.2 gap re-quoted" | **rejected as stated; "hypothesis not supported" + default-arm warm shift survive** — A/B/PSS/maps evidence undeposited, control-void rule fired, zen-contaminated baseline, 4.2 re-quote overreach | `da-logs/4.8.kitty-post-nvidia.md` |
| 9 | 4.8b kitty A/B (rerun per round 8) | scoped: hypothesis not supported; warm glvnd tax ~7-10 ms (interleaved, 2 boots, deposited, pre-reg committed pre-execution); PSS open | **survives with caveats** — false PSS retraction reversed (deposited mesa 72.7/72.8 caught by DA), stale 43 MB/11 ms purged, n=8 disclosed, fuzzel shift promoted to open question | `da-logs/4.8.kitty-post-nvidia.md` |
| 10 | 4.9 NixOS kitty ICD tax | scoped: NixOS pays the same glvnd tax (~10-12 ms on driver 595); 4.2's ~23 ms gap ≈ ICD tax stacked on a roughly 10-12 ms as-shipped OS gap | **survives with caveats** — rule's maps conjunct honestly relabeled (boot A unevaluated), stale-window probe forensics disclosed, PROBE-C fresh-boot mechanism added, readouts coarsened, kernel confound named | `da-logs/4.8.kitty-post-nvidia.md` round 3 |
| 11 | 4.14 kitty PSS method | "method rule adopted (first-window/fresh-boot/age>=120s → 102-106 MB); W2 ~250 MB informational" | **survives with caveats** — rule re-pinned to executed recipe + age window; both regimes quoted together (flattering-half warning); ordinal-vs-uptime confound + anon/file split demanded → 4.14b | `da-logs/4.14.kitty-pss-method.md` |
| 12 | 4.14b discriminating probe | "uptime/page-cache drives kitty PSS; anon constant ~51 MB = real cost; mechanism established" | **survives with caveats** — uptime-not-ordinal + file-locus stand; anon claim corrected (3 windows/2 boots, warm excess open), mechanism downgraded to strongly-indicated, convention re-pinned, 102-106 band disarmed pending 4.14c | `da-logs/4.14.kitty-pss-method.md` round 2 |
| 13 | 4.14c (E×3 asserted + drop_caches counterfactual) | "cache warmth causally established; anon floor 50.7-50.8; totals recipe-fragile" | **survives with caveats** — demonstrated-not-established (n=1), readahead claim struck over deposited null, anon n=6 deposited but right-censored (long-lived ≥54 MB leads BUDGET), 102-106 band retired as failed replication, rule 1 amended after third same-day violation | `da-logs/4.14.kitty-pss-method.md` round 3 |
| 14 | 4.16 kitty anon long run | "anon plateaus at 54.33 MB (bit-identical, ages 600-10800 s); 4.14's drift was a one-time settling step; censoring closed" | **survives narrowed** — one boot, one idle window, hours scale only (growth <~0.3 MB/h invisible); non-idle box disclosed and argued to strengthen (file-PSS responded, anon did not); spawn floor replicated n=7 | recorded in `4.16.kitty-anon-drift.md` (round in-file; margins pre-registered before data) |
| 15 | 4.18 oomd + load-protection | "oomd swap-kill canary passes (right cgroup, kernel OOM silent, no thrash); CPUWeight halves inflation; +core reservation ⇒ baseline; residual = SMT" | **survives with caveats** — condition D changed two variables (cores AND spinner count): recipe stands, SMT *isolation* downgraded to indicated (frequency probe rules out clocks); "zero thrash" softened (one tick stretched ≤1 s); pressure path configured-unexercised | `da-logs/overnight-2026-08-25.md` |
| 16 | 4.19 greetd | "≈6.8 MB PSS, two permanent processes; no detectable boot delta n=2v2; works first-try; class=greeter" | **survives with caveats** — the getty respawn-on-exit equivalence was untested inference → probed same round (`pkill niri` → fresh session <4 s, n=1); session-holder growth over long sessions open; portal re-acceptance under class=greeter already a pre-ship gate | `da-logs/overnight-2026-08-25.md` |
| 17 | 4.20 dir-aware hook + palette verdict | "hook 9.3 µs/prompt hot path, zero forks; matugen tones custom colors even with blend=false → envsubst wins (exact, 2.3 ms, zero new packages)" | **survives** — the syntax-artifact hole closed by probe (explicit table syntax, same toning); gettext verified `Required By: base pacman`; function-vs-integrated cost and 20-entry-map scaling noted (~0.4 ms/cd worst case, trivial) | `da-logs/overnight-2026-08-25.md` |
| 18 | 4.24 hands-on queue (7 conclusions A-G) | "MUB sweep complete/no collateral damage; nvidia-open no runtime delta; docker promotion done + cap re-proven; satty pipe proven; `pgrep -f` wrong; box is at the limine menu; check-limine catches the class" | **six of seven rejected or narrowed; only E survives as stated** — A: `-Rs` ignores optdepends, so "no collateral damage" was laundered, nothing launched, "complete" unmeasured. B: power + boot both control-void (n=1 vs n=1 across sessions; the "before" boot figure was the same run's second line), withdrawn. C: cap proven for `@dockervol` only at a synthetic 8 MiB via `dd`; **rollback isolation — the reason for the change — unexercised**; instrument lacked a nested-subvolume guard (added). D: proof image is a flat RGB(15,15,15) field identical to the wallpaper, so pass and fail were pixel-identical; bound command never tested; `slurp` cancel path silently broken. F: mechanism was inferred from `CONFIG.md`, which does not state it — **settled by reading `common/menu.c` v12.5.2 instead** (directory-as-default forces `skip_timeout`; Enter expands, never boots; `+` is load-bearing for numeric indexing; path form cannot select a folder) — box's *state* remains a ranked hypothesis, `hash_mismatch_panic` added as a competing one. G: **rejected — the script printed "is bootable, all booted files present" on two shapes it never inspected**; rewritten, self-test widened to three near-misses | `da-logs/4.24.hands-on-queue.md` |
| 19 | 4.25–4.27 overnight | eight conclusions: rollback gate passed, docker isolation, qgroup forced-readonly + repro, ENOSPC control, swaybg 8.2=RSS, wallpaper crops, greetd boot wash, limine directives reboot-verified | **three of eight survive as stated; four weakened, one rejected** — the ENOSPC "control" never refused a write so the cap-*introduces*-it argument is withdrawn; greetd boot gap re-quoted from n=1 vs n=1 (round 18's own error); `hash_mismatch_panic` wholly unexercised; "resolves" the 8.2 MB puzzle overstates a cross-machine correlation; docker rollback isolation confirms wiring rather than discovering it, and the pre-promotion-fstab case is untested | `da-logs/4.25-4.27.md` |
