# DA log — 4.8 kitty post-nvidia

## Round 1 (2026-08-24) — verdict: rejected as stated; narrow claim survives

Conclusion submitted: (1) author's cold-launch hypothesis refuted with sign
reversed; (2) mechanism = glvnd nvidia-ICD enumeration, ~+11 ms warm via
same-boot A/B; (3) +43.3 MB PSS same-boot; (4) cross-boot colds all above
envelope with control caveat; (5) 4.2 kitty gap re-quoted as ~11 ms.

DA verbatim:

1. **The load-bearing evidence is not in the raw file. This is disqualifying as it stands.** After the fuzzel void, the entire attribution — the write-up says so itself — "rests on the same-boot A/B." That A/B (mesa-pinned warm median 131.8, n=10), the PSS pair (181.2/137.9), the "47 nvidia entries" kitty maps count, the "73" niri count, and the "fuzzel maps zero" claim appear NOWHERE in `raw/4.8.kitty-post-nvidia.jsonl`, which the write-up cites as its raw and which contains "all raw samples tonight." The jsonl contains exactly three default kitty/fuzzel runs and nothing else. Every number the conclusion actually stands on is currently take-my-word-for-it, in a project whose whole methodology is built on depositable raw samples.
2. **The only mechanism probe that IS in the raw file failed.** The jsonl ends with `grep: /proc//maps: No such file or directory` and an output of `0` — an empty pid variable. Per the pre-registration's own rule, "zero nvidia mappings + a timing shift = flagged as mechanism-free." The recorded execution of the pre-registered probe literally returned zero. The "47 entries" figure comes from an undocumented re-run with no captured output.
3. **The pre-registered decision rule did not fire, and the headline pretends it did.** "Refuted, opposite sign" required "fuzzel unmoved." Fuzzel moved, 3/3 boots. The rule as written has no "cross-boot only" scoping; that scoping was invented after the data arrived. The pre-registered outcome is **VOID**, and the honest headline is "void; a post-hoc same-boot A/B suggests a glvnd tax."
4. **The author's hypothesis was about COLD launches, and there is no valid cold evidence left.** The cold sign-reversal is exactly the comparison the moved control voids, and the mesa run-0 "cold" is not a boot-cold. What survives: "not supported." What does not: "colds got slower because of nvidia." The verdict paragraph still quotes the voided cold numbers — smuggling them back under a caveat.
5. **The "pre-registered" baseline is contaminated with zen-kernel numbers while claiming "same kernel."** The 128.5 warm median is `battery.arch-zen-boot2.log` (zen kernel); fuzzel's 21.8 is also zen. Correct mainline envelopes: kitty warm 130.1–131.7, fuzzel 20.9–21.5. Consequently "mesa-pinned lands exactly in the pre-nvidia envelope" is false: 131.8 is above 131.7 under either envelope. A method-line violation inside the section whose purpose is rigor; biased against the claim, which saves the direction but not the credibility.
6. **The "pre-registration" is self-attested and untimestamped.** Written "while the runs were in flight"; both 4.8 files untracked in git; no independent evidence thresholds predate data. Also: the verdict header cited `da-logs/4.8.kitty-post-nvidia.md` — a file that did not exist. Citing a gate that hasn't run is exactly backwards.
7. **The A/B is a single unreplicated pair on an uncommitted instrument.** One boot, one n=10 per arm, no interleave, no replication. The sed-modified variant was never committed and the env value elided. Mitigating: the env(1) wrapper biases the measured tax low, so the direction is safe.
8. **Consequence 5 (the 4.2 annotation) overreaches.** Cross-night comparison of the exact class this write-up voided; "stacks matched" asserted not shown; no evidence NixOS glvnd enumerates the nvidia ICD the same way (`/run/opengl-driver` ≠ `/usr/share/glvnd`). "~11 ms" to one decimal off one A/B pair is false precision, already written into 4.2.
9. **The PSS story contains an unresolved 2× discrepancy that is quarantined, not explained.** Mesa-pinned tonight 137.9 MB vs pre-nvidia 75.3 MB — at least one night's PSS method is broken. The same-boot +43.3 MB delta survives internally, but it is a solo-window worst case; PSS splits shared pages across mappers, so the marginal cost per additional kitty is smaller. "Buy back 11 ms + 43 MB" must not imply linearity.
10. **"3 fresh boots" is asserted, not evidenced.** No boot-id/uptime in the markers; boots 2 and 3 report the identical niri socket name; settle protocol differs from baseline (load 0.04 starts imply undocumented idling).

**What survives cleanly:** the default-arm warm shift — 3 boots, tight distributions, medians 142.5/142.7/142.5 vs corrected mainline 130.1–131.7. Something real added ~11 ms to warm kitty launches after the nvidia install. What that something IS rests on unreplicated, undeposited evidence.

**Verdict: survives only as "hypothesis not supported"; the headline "refuted with sign reversed," the cold-launch claim, and the 4.2 "~11 ms honest gap" annotation are REJECTED as stated pending (a) deposit of the A/B, PSS, and maps raw data, (b) one interleaved/replicated A/B on a fresh boot, and (c) correction of the zen-contaminated baseline and the "lands exactly in the envelope" falsehood.**

Fixes applied same session: (c) done in place (correction left visible);
(a)+(b) via the 4.8b rerun, pre-registered in 4.8 and committed before the
rerun executed. Round 2 pending on 4.8b results.

## Round 2 (2026-08-24, on 4.8b) — verdict: survives with caveats

DA independently verified before criticizing: commit 5493977 (09:38:38)
contains the full pre-registration/corrected baseline/instrument; git diff
proves only results postdate it; raw boot timestamps (09:39:16, 09:41:09)
postdate the commit; corrected envelopes reconcile exactly with the
baseline raw; the four block deltas recompute correctly; maps counts
deposited with names, both boots.

DA criticisms, abridged (verbatim available in session transcript):

1. **"Mesa-arm PSS failed to deposit" is FALSE** — the raw contains
   `pss_kb=72790`/`72683` on split lines; the coherent deposited reading
   is a ~+65 MB nvidia PSS cost, and the "no RAM cost" framing trusted
   yesterday's already-impeached undeposited pair over tonight's deposited
   data. REJECTED; demanded a clean both-arms probe and reconciliation.
2. The retracted "43 MB" was still load-bearing in the author-decision
   framing twenty lines below its own retraction.
3. The 4.2 annotation stamped "DA-gated" on a stale ~11 ms; the gated
   range is 7-10 ms.
4. "Fires on every condition" glossed that the envelope condition is
   cross-night and degraded by the moved fuzzel control; survivable but
   must be stated. Power state of the baseline undeposited.
5. Timeline arithmetic strained; demanded journal excerpts (cheap).
6. The deposit was not yet in git — commit before quoting.
7. Warm n is 8/arm, not 10; the 166.3 outlier shows the cold tail extends
   past run 0, so the boot-cold n=1 pair sits on a warmup curve.
8. "Independently brackets" overstated round-1/rerun agreement (~1-2 ms
   unexamined drift).
9. Pre-registration proof had 38 s of two-clock margin; use minutes.
10. fuzzel's +1 ms is now 5/5 post- vs 4/4 pre-nvidia boots — a systematic
    boot-environment shift, promoted to open question (suspect: niri's own
    73 nvidia maps).

**Verdict: survives with caveats — the warm glvnd tax (7-10 ms, mechanism
deposited, pre-registration verified against git) is entered into
evidence; fixes required before quoting: PSS paragraph rewrite (1), purge
stale 43 MB / 11 ms quotes (2,3), commit raw+results (6), deposit journal
excerpts (5).**

Fixes applied same session, all four: PSS section rewritten around the
deposited 72.7/72.8 readings plus a clean addendum probe (which found the
delta grows with session age — +65-66 MB at ~130 s, +91 MB at ~650 s, and
absolute solo-kitty PSS unstable: mesa arm 72.7→171.5 MB within one boot);
stale figures purged from 4.8 consequences and the 4.2 annotation
(~7-10 ms); journal excerpts + power state + pid/cmdline deposited in the
raw addendum; everything committed. Standing consequences: no kitty PSS
absolute is quotable until a steady-state method is settled; fuzzel/niri
post-nvidia shift is an open question every future cross-night envelope
inherits.

## Round 3 (2026-08-24, on 4.9 — NixOS ICD tax) — verdict: survives with caveats

Conclusion submitted: NixOS pays the same glvnd tax (Δ 12.0/10.1 ms, 2
boots, pre-reg commit 3dfc8d2 before execution); maps 51-vs-0; symmetric
cross-OS readouts.

DA highlights (abridged): (1) the pre-registered rule as written did NOT
fire — the maps conjunct had zero valid evidence on boot A; "per the
pre-registered rule" was the same sin 4.8 r1 rejected. (2) The
"controlled reprobe"'s own pid ordering falsified its claimed D-P-P-D
protocol: default1 (pid 2812) and PROBE-FIX default (2864) were STALE
windows orphaned by the broken in-run probes killing wrong pids — honest
fresh count 1×default + 2×pinned; direction survives on pid-ordering
evidence, narrative does not. (3) The write-up misdescribed its own probe
defect ("empty pid" — pids were present, /proc reads empty). (4) No
wall-clock anywhere in the deposit; the claimed timeline arithmetically
strained; every future raw gets wall-clock per boot marker. (5) The
"anomalous block" is two contaminated runs, diluted not laundered;
understates the tax, overstates the pinned-vs-pinned gap ~0.6 ms. (6)
"6 boots" is 5; mixed median estimators; coarsen quoting. (7) The
"OS-intrinsic" attribution must name the kernel confound (7.1.9 vs
6.18.44). (8) fuzzel "no drift" is a judgment call vs an n=2 envelope;
helping observation: fuzzel flat while kitty slower localizes the gap to
kitty's path.

Verdict: survives with caveats — timing delta and tax existence stand;
demanded before the 4.2 annotation: honest rule accounting, reprobe
rewrite matching its own pids, a fresh-boot environ-verified maps probe,
5-boot correction, coarsened symmetric readouts with kernel confound.

Fixes applied same session, all: text corrections in place; PROBE-C run
on a third fresh boot (wall-clock + journal anchors, preexisting-windows
check, single-window invariant, environ verified: default 51 maps /
pinned 0) — mechanism now verified on two boots cleanly; 4.2 annotation
written at coarse precision only. Standing rule adopted: every raw
deposit carries wall-clock lines per boot marker.
