# DA log — docker storage quota (4.5), round 1

DA agent: fresh Sonnet, nix-bunne briefing role. It read the write-up
(rev 1), the instrument, the raw log, both CHOICES rows, and the 4.2
da-log for precedent. **Verdict: rejected outright pending rerun.**

Substance of the eleven findings (full text in the agent transcript):
(1) the load-bearing rescan/refusal/wedge/recovery claims had no raw
backing — an unlogged SSH session, the same defect 4.2 was convicted of
twice the same night (the follow-up raw file was committed minutes after
this DA launched and it never saw it — but its harder demands stood
regardless); (2) the raw log's last captured state still showed qgroups
inconsistent/0 bytes; (3) n=1 on enforcement; (4) refusal arithmetic
inferred, no du/qgroup at the refusal moment; (5) recovery "works
normally" asserted without logged output; (6) the rev-1 raw log's
recovery_ok=1 predates the rescan and could mislead; (7) **routine
snapper snapshots might re-mark qgroups inconsistent and silently disarm
the cap — never checked**; (8) the tested path was a cp -a migration, not
the fresh-subvolume recipe handed to install.sh; (9) 537 MB/image is a
zero-sharing worst case stated as fact; (10) volumes untested but the
headline doesn't carry that weight; (11) evidence-quality varied silently
between raw-backed and recalled claims. Solid and kept: Phase A directory
attribution, trap #1 (silent non-enforcement pre-rescan), snapshot
exclusion.

## Response — the 4.5b rerun (all demands, fully tee'd)

`instruments/4.5b.docker-quota-rerun.sh`, raw
`raw/4.5b.docker-quota-rerun.log`: docker-path refusal reproduced (n=2);
**trap-7 answered — a snapper snapshot does not re-mark qgroups
inconsistent, and the cap refused the very next fill**; wedge reproduced
and the bump-clean-relimit recovery logged end-to-end with a post-recovery
consistency check, import, and delete; the production recipe run on two
fresh empty subvolumes, both enforcing at the fourth write; enforcement
observed to act on reservations ahead of lazy qgroup accounting; new wart
recorded — containerd content GC is lazy (~1 GB residue after deleting all
images), so cap sizing must include GC headroom. Points 9-11 fixed as
labels/text in the write-up (worst-case label, volumes caveat kept
prominent, evidence-quality note). Point 6: the stale recovery_ok=1 is now
explained in place. Rows remain `deferred` — the author settles the
design; only the evidence changed.


# Round 2 (on revision 2 + the 4.5b rerun)

**Verdict: survives with caveats — text fixes only, "no further rerun
needed."** DA verified script/log fidelity line-by-line ("every script line
traces exactly to a matching log line") and called the fresh-subvolume
recipe test "the strongest and most trustworthy new evidence." Residuals,
all conceded and fixed in place: (1) "docker-path refusal n=2" laundered
the reconstructed rev-1 instance into parity with the tee'd trap-7 one —
relabeled: one verified instance, rev-1's superseded; boundary arithmetic
now walked through in text; (2) the promised stale-`recovery_ok=1`
explanation was missing — added at Phase C; (3) the GC wart was not new,
only newly quantified — reframed (residue grew 505 → 1019 MiB across a
second cycle); (4) per-claim evidence labels added to the Phase B refusal
paragraph. Trap-7's n=1 labeling checked and found honest.
