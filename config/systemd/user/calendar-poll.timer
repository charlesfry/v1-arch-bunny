[Unit]
Description=Check the calendar every minute for reminders due to fire

[Timer]
OnCalendar=minutely
# Right after a resume/reboot, check immediately rather than waiting up to a
# minute -- same reasoning as disk-usage-alert's Persistent=true.
Persistent=true

[Install]
WantedBy=timers.target
